import { Vector2 } from "../math/vector2";

class Projectile {
    private position: Vector2;
    private velocity: Vector2 = Vector2.zero;
    private force: Vector2 = Vector2.zero;

    constructor(position: Vector2) { this.position = position }

    
}